export type Timer = {
    id: number;
    name: string;
    exerciseDuration: number;
    restDuration: number;
    rounds: number;
}

export type Credentials = {
    email: string;
    password: string;
}